# Server
NODE_ENV=development
PORT=3000

# Database (MongoDB). Use with: npm run seed (loads sample data)
DATABASE_URL=mongodb+srv://tom:3VcA5JmnEEwwGAFh@cluster0.4g3zvom.mongodb.net/news?appName=Cluster0

# JWT
JWT_SECRET=your-super-secret-jwt-key-min-32-chars
JWT_EXPIRES_IN=24h

# Optional: Read deduplication window (minutes) - same reader+article within this window = one read
READ_DEDUP_WINDOW_MINUTES=15
